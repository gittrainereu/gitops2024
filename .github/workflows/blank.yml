name: Share data between jobs

on: [push]

jobs:
  is-2nd-lang-changed:
    name: langtest
    runs-on: ubuntu-latest
    steps:
      - shell: bash
        env:
          SUPER_SECRET: ${{ secrets.SECRET_SUPER_SECRET }}
        run: |
          echo "$SUPER_SECRET" 
          echo "foo" > /tmp/myfile 
          echo "OUTPUT 1: -------"
          cat /tmp/myfile 
          echo "EOF OUTPUT1"
          echo "OUTPUT 2: -------"
          echo "$SUPER_SECRET" >> /tmp/myfile 
          echo "EOF OUTPUT 2"
          cat /tmp/myfile 
          echo "OUTPUT 3: -------"
          echo "foo2" >> /tmp/myfile 
          cat /tmp/myfile 
          echo "EOF OUTPUT 3"
